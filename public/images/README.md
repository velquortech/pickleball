# Placeholder artwork

These are branded SVG illustrations standing in for real facility photos.
When photography is available, drop the photos here and swap the `src`
values — keep the same filenames (or update the imports) and roughly a
16:10 aspect ratio so the layouts hold.

| File | Used in |
|---|---|
| `open-play.svg` | Landing → What's On card 1 |
| `events.svg` | Landing → What's On card 2 |
| `coaching.svg` | Landing → What's On card 3 |
| `court-day.svg` | Landing → About collage |
| `queue-board.svg` | Landing → About collage |

Palette (Vibrant Athleticism): deep navy `#0b1626`/`#111c2d`, panel `#152031`,
neon lime `#caf300`, electric blue `#38bdf8`, action orange `#ff7a3d`,
lavender `#bec6e0`, light text `#d8e3fb`.
